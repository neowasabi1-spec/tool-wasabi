feat(funnel-preview): live diagnostic HUD + re-run fallback button

- Inject visible HUD overlay inside cloned-page iframe (top-right) showing
  fallback state (boot/libs-ready/after-fallback/retry), jQuery/Swiper
  presence, FAQ/Swiper/slide/thumb counts and last runtime error.
- Add 'Re-run fallback' button in the modal preview tabs that posts a
  'rerun' command to the iframe, useful when first init misses tardy DOM.
- Surface diag messages as a colored badge in the modal tab bar (no need
  to open DevTools to verify the fallback executed).
- Capture iframe runtime errors via window.error listener and forward
  them to parent so the user sees CSP/CDN failures directly in the UI.
- Broaden FAQ selectors to cover more accordion patterns (details,
  .faq-item .faq-title, .accordion-item > button).
